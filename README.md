# TimerPro
TimerPro is a countdown timer application built with React and TypeScript. Users can set a custom time in HH:MM:SS format, start, stop, and reset the timer. This project was created to learn React fundamentals including components, state management, event handling, and interval-based updates.
